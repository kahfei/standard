<h1>Editing user</h1>
  
<% if session[:user_section] == @user.section || session[:user_role] == "Superadmin" || session[:user_role] == "Departmenthead" && @user.role == "Departmenthead" || session[:user_role] == "Departmenthead" && @user.role == "Sectionhead" %>
	<%= render 'form' %>

	<%= link_to 'Back', users_path %>
<% else %>
	<h2>You only have permission to edit users of your section!</h2>
<% end %>